## TFL Application

This is Data structures coursework for finding the shortest path

## Requirement

1. C# >= 8
2. Excel sheet containing the stations

## To Run the Application

Update the path to the excel sheet

## Excel Data

The data used in this project is stored in an Excel sheet located at `"DAS Coursework/data/GetData.cs"`. To access this data, follow these steps:

1. Navigate to the `DAS Coursework/data/` directory within the project.
2. Open the file named `GetData.cs` using your preferred spreadsheet software.
3. You can now view and work with the data contained in the Excel sheet.
